// Planning Poker — Public Configuration
//
// cloudProjectNumber is a public identifier (visible in browser source).
// It is safe to commit. See docs/architecture.md for details.
//
// If you ever add a backend (Firebase, Cloud Functions, etc.), put service
// account keys and API secrets there — never in this file.

const CONFIG = {
  cloudProjectNumber: 'YOUR_CLOUD_PROJECT_NUMBER',
};
